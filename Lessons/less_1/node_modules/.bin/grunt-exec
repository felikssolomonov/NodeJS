#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-exec/bin/grunt-exec" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-exec/bin/grunt-exec" "$@"
  ret=$?
fi
exit $ret
